# Phase 2 exit review — 2026-08-30

Full-tree review pass per plan 06 §8, executed by an agent reviewer running
`agents/code-review.md` against the Phase-2 delta (subscriptions, callbacks,
async adapters, soak/JMH/Windows wiring), followed by resolution of every
finding and re-greening of both gates (55 tests).

## Gates (both green after resolution)

- `just verify`: fmt, clippy -D warnings, cargo test, `mvn verify` — 54 tests,
  SpotBugs 0 bugs, JaCoCo zero-coverage met, Checkstyle/Spotless clean.
- `just review`: all of the above plus machete and the allows-grep.
- `just bench`: builds the shaded JMH jar end-to-end (library install →
  module package); nightly workflow runs soak + benchmarks.
- CI: verify matrix now includes windows-latest.

## Findings and resolutions (first pass: REQUEST CHANGES, 17 findings)

1. `[SLOP]` Leftover `eprintln!` in the worker loop — deleted.
2. `[TESTS]` Flaky SpikeB floor assertion — now asserts delivery works and
   prints the (machine-dependent) ratio; docs/benchmarks.md records the
   number as unstable, never gated.
3. `[INVARIANT]` Query-callback workers emit no exhaustion signal at the pin
   — recorded as a lifecycle contract: `onClose` fires on cancel/error/
   listener-throw; documented in `CallbackSubscription` Javadoc.
4. `[INVARIANT]` `deliverData` could return null addresses → JVM crash —
   the bridge now refuses (throws) once stopped; the worker re-checks stop
   before exporting; refusal paths deliver close without a second onError.
5. `[INVARIANT]` Container/registration leaks — single teardown path
   (`unregister` closes containers) invoked from close, deliverError,
   deliverClose, and `start()`'s failure path; callback cycles added to the
   nightly soak.
6. `[INVARIANT]` `deliverError` built the base exception — now routed
   through a Java mirror of the Rust code→class table, itself table-tested
   (`CallbackBridgeErrorsTest`).
7. `[DEAD]` Seven unexercised public members — `SurfaceCoverageTest` gained
   queryAsync/asyncExecutor, streamBatches default, named-stream batch
   conveniences, callback isActive lifecycle.
8. `[DEAD]` Broken masked JMH wiring — standalone module with shade plugin,
   `just bench` installs the library first and propagates failures, nightly
   benchmarks job added, both broken benchmarks fixed (unique primary keys;
   real deliveries before cancel).
9. `[DOCS]` Review record claimed before it existed — written (this file);
   checklist wording corrected.
10. `[DOCS]` threading.md lag mechanism wrong — corrected: lag surfaces as a
    subscription error that terminates the subscription (`nextFrame()`
    throws; callbacks get `onError`+`onClose`).
11. `[DOCS]` stateful-and-joins.md stale — reading-output section now
    documents the shipped subscription API; the emission observation is
    recorded where plan 03 §7 cites it; the temporal-join sentence matches
    the api-only decision.
12. `[DOCS]` 64-subscription cap undocumented — Javadoc + threading.md;
    thread-contract table grown for the subscription classes.
13. `[TESTS]` Debug prints + `sample` tooling residue in SubscriptionTest —
    deleted.
14. `[TESTS]` Soak gaps — checkpoint assertion added; per-thread writers per
    the documented contract; concurrent checkpointing added; callback-cycle
    soak added.
15. `[INVARIANT]` Spurious-wakeup-unsafe join — deadline-bounded wait loop
    reporting only the flag.
16. `[INVARIANT]` `streamBatches` close contract — `.onClose(this::close)`
    wired; Javadoc updated.
17. `[DEAD]` Stale JaCoCo exclusion for `LaminarSubscriptionException` —
    class now exercised (closed-subscription 501 test); exclusion removed.

Non-blocking notes adopted: nits fixed inline (deadline comment, "slower"
wording, queryAsync ownership Javadoc, ALLOCATOR comment wording). Notes
deferred with rationale: `ACTIVE` counter uses compare-exchange-grade
exactness only if the cap becomes load-bearing (recorded here); panic-path
`deliver_close` (the exit signal already releases joiners); the
`ArrowBatch.delivered` seam stays public-but-internal for now.

## Exit state

- Open REQUEST CHANGES findings: **0**.
- Time-gated: the soak week (`-alpha` → `-beta`) starts at merge — nightly
  workflow wired.
- Maintainer blockers unchanged from Phase 1 (Maven Central credentials;
  plan 04 §5).
