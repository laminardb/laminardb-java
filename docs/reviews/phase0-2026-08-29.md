# Phase 0 exit review — 2026-08-29

Full-tree review pass per plan 06 §8, executed by an agent reviewer running
`agents/code-review.md` against the tree at commit `44d6fdf` (scaffold +
gates), followed by resolution of the findings at commit `89dd091`
("fix: address phase-exit review findings on error mapping and docs").
Scope: everything plan 01 ships — Rust cdylib (`src/*.rs`), Java API
(`src/main/java`), build wiring (`justfile`, `pom.xml`, `.cargo/`,
`.checkstyle.xml`), CI (`.github/workflows/ci.yml`), `AGENTS.md`, and the
plan-01 corrections themselves.

## Gates (both green, locally and in CI)

- `just verify`: fmt --check, clippy --all-targets -D warnings, `cargo test`
  (2 FFI roundtrip tests), `mvn test` (10 JUnit: 7 smoke + 3 Arrow spike) —
  exit 0.
- `just review`: fmt, clippy, `cargo machete` (clean), allows-grep (both
  `#[allow]`s carry WHY + phase), Spotless + palantir-java-format (clean),
  Checkstyle (0 violations) — exit 0.
- CI run `33250001085` (commit 44d6fdf): all six jobs (rust-lint, review,
  verify × ubuntu-latest x86_64, macos-latest aarch64) green from clean
  checkouts — the pinned `v0.30.0` git-tag core dep resolves with no sibling
  clone and no system packages beyond the toolchains.

## Findings and resolutions

First pass verdict: **REQUEST CHANGES** — 5 findings, all resolved:

1. `[DOCS]` Task 0.6 checkbox claimed branch protection that was not yet
   enabled. **Resolved**: protection enabled after the final CI-green push
   (see "Exit state" below); the checkbox now reflects reality.
2. `[DOCS]` `LaminarDB.open()` documented `@throws LaminarConnectionException`,
   impossible at the pin (engine-start failures map to code 900; verified
   against `ApiError::from(DbError)` at v0.30.0). **Resolved**: `@throws
   LaminarException` with the code-900 note.
3. `[DOCS]` `error.rs` comment claimed init retry while `OnceLock<Option<_>>`
   cached failure forever — and the cached `None` would have silently
   swallowed every future throw. **Resolved structurally**: `OnceLock<MappedExceptions>`
   with re-attempt on miss; failed inits leave the cell empty so the next
   throw retries. Comment now matches behavior.
4. `[DOCS]` `AGENTS.md` and `Native.java` still described the internal seam as
   "package-private / never referenced outside this package" after the
   visibility correction. **Resolved**: both say "referenced only by
   `io.laminardb`, never by user code"; AGENTS.md states the package is the
   boundary.
5. `[DOCS]` Missing Javadoc on the two public exception constructors.
   **Resolved**: one-line ctor Javadoc on both.

Non-blocking notes adopted in the same commit: dropped `cargo-machete` from
CI's `verify` job (only `review` uses it); aligned AGENTS.md's `just test`
wording with the pom `laminardb.native.dir` mechanism; wrapped the spike
tests' `CDataDictionaryProvider` in try-with-resources; renamed
`versionTracksThePinnedCore` → `versionIsSemver` (the assertion is a semver
match, not a pin check). Notes not adopted: none rejected.

## Areas verified clean (reviewer-confirmed, not assumed)

Handle-lifetime race analysis (Java lock held across the native execute; close
nulls under the lock, frees outside it; native `Mutex<Option<Connection>>`
takes exactly once — single-free and use-after-close→101 hold); Arrow C Data
Interface ownership both directions (`markReleased()` after Rust `from_ffi`
consumes; importer owns post-import); all six JNI exports have Java callers;
no TODO/stubs/commented-out code/attribution; conventional-commit history; no
reflection/mock/`Thread.sleep` tests and all loops bounded; no Phase 1+
surface (no `Binding` SPI, no config, no `QueryResult`); plan-01 corrections
match observed behavior at the pin (duplicate CREATE SOURCE → 400, blocking
subscribe/next_frame reject runtime context).

## Exit state

- Open REQUEST CHANGES findings: **0**.
- Branch protection on `main` enabled after the final green push: PR reviews
  required, direct pushes blocked, the six matrix CI checks required.
- Plan 01 status header set to `Implemented (2026-08-29)` with the acceptance
  checklist ticked; README bootstrap status updated.
